package com.harel.cookle.controllers;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

/**
 * Web controller that handles mapping of web pages in the application.
 * This controller is responsible for serving the main search page.
 */
@Controller
public class WebController {

    // /**
    //  * Maps the /project path to the main recipe search page.
    //  * 
    //  * @return Forward directive to the index.html page
    //  */
    // @GetMapping("/project")
    // public String projectPage() {
    //     return "forward:/index.html";
    // }
} 